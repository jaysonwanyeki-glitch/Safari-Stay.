# Postgres connection string (Neon, Supabase, Railway — any Postgres works).
# The schema is created with `npm run db:push`, data with `npm run db:seed`.
DATABASE_URL=postgres://user:password@host/db?sslmode=require
